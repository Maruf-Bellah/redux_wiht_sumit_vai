import React from 'react';
import '../../App.css'

const State = ({count}) => {
    return (
        <div className='first-div'>
                <h1>Total Count = {count}</h1>
        </div>
    );
};

export default State;